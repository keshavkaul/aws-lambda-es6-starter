{
  "name": "aws-lambda-es6-starter",
  "version": "1.0.0",
  "description": "A starter template for writing es6 javascript lambda code.",
  "main": "./bin/index.js",
  "scripts": {
    "build": "del-cli 'bin/*' '!.gitkeep'; babel src -d bin",
    "pack2zip": "npm pack; tar2zip *.tgz; del-cli '*.tgz'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.5.1",
    "del-cli": "^1.0.0",
    "tar-to-zip": "^1.3.4"
  },
  "bundledDependencies": []
}
